## 2024-06-11 - Theme Toggle Accessibility
**Learning:** Icon-only buttons used for theme toggling lack ARIA labels, making them inaccessible to screen readers.
**Action:** Always ensure that icon-only interactive elements, such as theme or language toggles, have an `aria-label` attribute that clearly describes their action.
